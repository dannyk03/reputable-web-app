{
  "name": "@reputable/types",
  "version": "1.0.0",
  "description": "Common types between web and api",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "start:prod": "rm -rf dist && tsc",
    "build": "rm -rf dist && tsc",
    "dev": "rm -rf dist && tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "config": "*",
    "tsconfig": "*",
    "typescript": "^4.6.3"
  }
}
